# prep_output_analyze 평가 설정
# Phase 1: Q&A + Survey → question_context 분석

name: prep_output_analyze
output_format: json
evaluators:
  - type: llm_judge
    enabled: true
    criteria:
      - meeting_prep/analyze_quality_classification
      - meeting_prep/coaching_hint_specificity
      - oneonone/sensitive_topic_handling

thresholds:
  pass_rate: 0.85
  min_score: 0.70

run_mode: full
